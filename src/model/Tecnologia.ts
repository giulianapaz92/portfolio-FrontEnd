export default class Tecnologia {
    id: number;
    nombre: string;
    nivel: string;
    
    constructor(id: number, nombre:string, nivel:string) {
        this.id = id;
        this.nombre = nombre;
        this.nivel = nivel;
    }

  }
